# GitHub publication metadata

Prepared locally. Do not publish until the repository owner approves the final release.

## Repository identity

- Name: `sms-esp-rat`
- About: `Codex skill for token-efficient Spanish technical answers with semantic compression, integrity guards, and reproducible benchmarks.`
- Social preview: `assets/social-preview.png` (1280 × 640 PNG, solid background, under 1 MB)
- Generation prompt: `assets/social-preview.prompt.md`
- Tagline: `Less noise. Same signal.`
- Website: leave empty unless a maintained project page exists.
- License: MIT, matching the complementary `token-rat-esp` repository.

## Recommended topics

```text
codex
codex-cli
codex-skills
agent-skills
ai-agents
llm
prompt-engineering
token-optimization
token-reduction
semantic-compression
text-compression
spanish
developer-tools
benchmarking
python
```

These 15 topics are lowercase, hyphenated where needed, and below GitHub's limit of 20 topics. They combine broad discovery terms with the project's specific function.

## Apply after creating the remote

Replace `OWNER` and run only after reviewing the target repository:

```bash
gh repo edit OWNER/sms-esp-rat \
  --description "Codex skill for token-efficient Spanish technical answers with semantic compression, integrity guards, and reproducible benchmarks." \
  --add-topic codex \
  --add-topic codex-cli \
  --add-topic codex-skills \
  --add-topic agent-skills \
  --add-topic ai-agents \
  --add-topic llm \
  --add-topic prompt-engineering \
  --add-topic token-optimization \
  --add-topic token-reduction \
  --add-topic semantic-compression \
  --add-topic text-compression \
  --add-topic spanish \
  --add-topic developer-tools \
  --add-topic benchmarking \
  --add-topic python
```

Upload `assets/social-preview.png` manually through **Settings → General → Social preview → Edit**.

---

# Metadatos de publicación en GitHub

Preparados localmente. No publiques hasta que el propietario del repositorio apruebe la release final.

## Identidad del repositorio

- Nombre: `sms-esp-rat`
- Descripción: `Codex skill for token-efficient Spanish technical answers with semantic compression, integrity guards, and reproducible benchmarks.`
- Vista previa social: `assets/social-preview.png` (PNG de 1280 × 640, fondo sólido y menos de 1 MB)
- Prompt de generación: `assets/social-preview.prompt.md`
- Lema: `Less noise. Same signal.`
- Web: déjala vacía mientras no exista una página mantenida del proyecto.
- Licencia: MIT, como el repositorio complementario `token-rat-esp`.

## Etiquetas recomendadas

Usa los 15 topics de la sección inglesa. Combinan búsquedas amplias con la función específica del proyecto sin superar el límite de GitHub.

## Aplicación tras crear el remoto

Sustituye `OWNER` en el comando anterior y ejecútalo solo después de comprobar el repositorio de destino. La vista previa social se carga manualmente desde **Settings → General → Social preview → Edit**.

Antes de hacer público el repositorio:

1. Ejecuta las pruebas y el validador de la skill.
2. Revisa que el README renderice el banner y las ecuaciones correctamente.
3. Confirma que no haya secretos, cachés ni resultados privados.
